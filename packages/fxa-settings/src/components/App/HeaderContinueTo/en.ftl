## HeaderContinueTo component

# This message directly follows a task header (e.g., 'Reset password')
# If more appropriate in a locale, this can stand alone as "Continue to { $serviceName }"
header-continue-to-subheading-default = to continue to account settings

# This message directly follows a task header (e.g., 'Reset password')
# If more appropriate in a locale, this can stand alone as "Continue to { $serviceName }"
# { $serviceName } variable represents a product (e.g., Mozilla VPN)
header-continue-to-subheading-custom = to continue to { $serviceName }
